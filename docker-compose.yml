version: '3.1'

services:
  service:
    environment:
      - KAFKA_BROKER_LIST=http://localhost:9090
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
    expose:
      - 8080
